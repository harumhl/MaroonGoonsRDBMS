#include <iostream>
#include "Engine.h"
#include "League.h"
using namespace std;

int main() {
  Engine* engine = new Engine();
  Parser* parser = new Parser();
  
  while (true) {
    string line;
    getline(line, cin);
   
    // parsing
   
    // executer
  }
}
